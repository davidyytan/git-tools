Title guidance:
- The Pull Request title is fixed for this mode.
- Use exactly `## Title: {fixed_title}` in the output.
- Do not generate an alternative title or vary the title wording.
- Use this mode for the first PR on a fresh develop line when it must land as `Start X.Y.Z`.
- Treat the body like a normal develop PR: summarize the actual changes from the commits, diffs, and context.
- Good examples: `Start 0.3.0`, `Start 1.7.0`, `Start 2.0.0`.
- In the body, also explain that merging this PR opens the named alpha line on `develop`.
- Do not invent QA results, rollout steps, or release notes not supported by the available artifacts.
